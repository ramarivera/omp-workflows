{
  "stable-replay": ["longest unchanged prefix", "changed suffix reruns"],
  "nested-replay": ["run IDs namespace call index"],
  "crash-after-dispatch": ["unknown or reconciled, never silent duplicate"],
  "unknown-reconciliation": ["stable child session inspection"],
  "pause": ["no new dispatch", "running calls finish"],
  "stop": ["abort active", "late result rejected"],
  "stale-controller": ["late writes rejected", "reload handoff"],
  "strict-schema": ["terminating structured output failure"],
  "permissive-schema": ["warning and payload retained"],
  "all-settled-fan-out": ["partial success retained"],
  "fatal-fan-out": ["cooperative siblings aborted"],
  "shared-concurrency": ["nested active count bounded"],
  "agent-limit": ["max agents enforced"],
  "token-runtime-limit": ["typed limit and partial results"],
  "underlying-timeout": ["child session aborted"],
  "model-routing": ["requested/resolved/fallback metadata"],
  "named-toolset-reresolution": ["names resolved after restart"],
  "isolation-fail-closed": ["no shared-tree fallback"],
  "capture-only": ["parent worktree unchanged"],
  "approval-invalidation": ["hash binds script args policy version"],
  "argument-validation": ["reject before agent dispatch"],
  "primary-backup-recovery": ["backup restores corrupt primary"],
  "persistence-degraded": ["visible degraded state"],
  "lease-ownership": ["owner token required"],
  "cross-platform-storage": ["mac/linux/windows paths and atomic writes"],
  "session-reload-continuity": ["inspect after restart/new session"],
  "invalid-control-transition": ["allowed actions and unchanged state"],
  "research-provenance": ["URL shape and distinct support counts"]
}
